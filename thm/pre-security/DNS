# Pre-Security / DNS

## What is DNS?
  

## Record
- A: IPv4
- AAAA: IPv6
- CNAME: alias
- MX: poczta
- TXT: tekst (np. SPF/DKIM)
- NS: serwery nazw

## Flow zapytania (mini)
Client → Recursive Resolver → Authoritative → answer → cache (TTL)

## Tools
- nslookup / dig

## 5 typowych problemów
1) zły rekord / zły typ
2) cache i TTL
3) brak propagacji / stary resolver
4) zły CNAME (pętla)
5) błędne NS / delegacja
